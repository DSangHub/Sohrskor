export default function Home() {
  return (
    <main className="min-h-screen bg-white">
      {/* Hero */}
      <section className="text-center py-20 px-4 bg-gradient-to-b from-blue-50 to-white">
        <h1 className="text-5xl font-bold text-gray-900 mb-4">
          What's Your Home's <span className="text-blue-600">SOHR Score</span>?
        </h1>
        <p className="text-xl text-gray-600 mb-8">
          Get it free in 2 minutes. No purchase. No inspection.
        </p>
        
        {/* Address Input — The Hook */}
        <div className="max-w-2xl mx-auto flex flex-col sm:flex-row gap-3">
          <input 
            type="text" 
            placeholder="Enter your address (e.g., 123 Main St, Austin, TX)"
            className="flex-1 px-6 py-4 border border-gray-300 rounded-xl text-lg shadow-sm focus:ring-2 focus:ring-blue-500"
          />
          <button className="px-8 py-4 bg-blue-600 text-white text-lg font-semibold rounded-xl hover:bg-blue-700 transition shadow-lg">
            Get My Free Score →
          </button>
        </div>
        <p className="text-sm text-gray-400 mt-4">No credit card. No obligation. No spam. Ever.</p>
      </section>

      {/* 3 Value Boxes */}
      <section className="max-w-6xl mx-auto py-16 px-4 grid md:grid-cols-3 gap-8">
        {[
          { icon: "📊", title: "Your Score, Instantly", desc: "0–100 rating of your roof, HVAC, windows, floors, and more" },
          { icon: "📈", title: "Neighborhood Edge", desc: "Even in a medium-demand area, a higher score means more value" },
          { icon: "🛠️", title: "Clear Next Steps", desc: "We tell you the 2–3 upgrades that boost your score most" }
        ].map((box, i) => (
          <div key={i} className="p-6 bg-gray-50 rounded-2xl text-center">
            <div className="text-4xl mb-3">{box.icon}</div>
            <h3 className="text-xl font-semibold mb-2">{box.title}</h3>
            <p className="text-gray-600">{box.desc}</p>
          </div>
        ))}
      </section>

      {/* Trust Bar */}
      <section className="bg-gray-900 text-white py-8 text-center">
        <p className="text-lg">✅ No purchase required &nbsp;·&nbsp; 🔒 Your data is private &nbsp;·&nbsp; 💯 100% free</p>
      </section>
    </main>
  );
}
